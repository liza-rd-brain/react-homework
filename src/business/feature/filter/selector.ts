import { StoreType } from "@/business/store";

export const selectFilterModule = (state: StoreType) => state.filter;

// export const selectFilter = (state: StateType) => selectFilterModule(state);
